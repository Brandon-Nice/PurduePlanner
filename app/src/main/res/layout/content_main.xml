<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainActivity"
    android:weightSum="1"
    tools:showIn="@layout/activity_main">

    <ImageButton
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:id="@+id/scheduleButton"
        android:background="@drawable/schedule2"
        android:contentDescription="scheduleImage"
        android:clickable="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_gravity="bottom" />

    <ImageButton
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:id="@+id/mapsButton"
        android:background="@drawable/maps"
        android:clickable="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/scheduleButton"
        android:layout_gravity="bottom" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/friendButton"
        android:background="@drawable/friend"
        android:layout_alignTop="@+id/scheduleButton"
        android:layout_alignBottom="@id/scheduleButton"
        android:layout_toRightOf="@+id/scheduleButton"
        android:layout_toEndOf="@+id/scheduleButton" />

</RelativeLayout>
